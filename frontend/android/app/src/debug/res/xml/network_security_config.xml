<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug builds only. Android 9+ blocks cleartext HTTP, but during development the API
  runs as plain http on the developer machine. Release builds do not include this file
  and stay HTTPS-only, which is what the Cloudflare Tunnel serves.

  10.0.2.2 is how the Android emulator reaches the host. Add your machine's LAN IP
  here to test from a physical phone on the same network.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
