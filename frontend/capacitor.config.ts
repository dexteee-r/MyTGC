import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  // Fixed to match PROJECT_CONTEXT.md section 1. The scaffold this was extracted
  // from carried 'be.elmzn.onepiecetracker'.
  appId: 'be.elmzn.mytgc',
  appName: 'MyTGC',
  webDir: 'dist'
};

export default config;
