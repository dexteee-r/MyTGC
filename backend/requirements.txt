# Python 3.14. Versions are lower bounds, not pins: 3.14 is recent enough that
# older releases have no wheels and fall back to building from source, which fails.

# Build step 1 (catalogue import) uses the standard library only.

# Build step 3 — image download + perceptual hashing
requests>=2.34
Pillow>=12.3
ImageHash>=4.3.2          # pulls numpy, scipy, PyWavelets

# Build step 4 — card detection / deskew
opencv-python-headless>=5.0        # ships a cp37-abi3 wheel, works on 3.14

# Build step 6 — API
fastapi>=0.141
uvicorn[standard]>=0.52
# python-multipart                 # only needed once /scan exists
